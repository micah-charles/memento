# MEMENTO package artwork

These PNGs are the checked-in package artwork used by `Build-MementoMsix.ps1` for the Store logo, square app tiles, and splash screen. They are deterministic local assets so MSIX inspection no longer depends on generated 1×1 placeholders. Replace them only after reviewing the publisher-owned product branding and update the package verification evidence with the new asset dimensions and hashes.
