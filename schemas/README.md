# Versioned data contracts

The schemas here are documentation-level contracts for review. They are not yet wired to an application. The archive format must remain exportable as JSON/JSONL plus ordinary media files, with `schema_version` carried on every durable record.
