# `src/`

The implementation lives here. `Memento.Core` contains the local SQLite archive, crash-safe audio, provider boundaries, language validation, and clarification provenance; `Memento.App` contains the minimal WinUI 3 shell. Live provider credentials, physical microphone verification, and participant UX review remain deployment gates.
