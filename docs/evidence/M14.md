# M14 evidence — portable deployment and pilot preparation

**Date:** 2026-09-14  
**Status:** PREPARATION / AUTOMATED VERIFIED; supervised pilot gates pending

## Deployment verification

- `scripts/Publish-Memento.ps1` produced a self-contained Windows x64 bundle at `artifacts/MEMENTO-win-x64.zip`.
- Bundle size: **109,614,610 bytes**.
- SHA-256: `27c0f389ece00edbc7b9649818885ba771afb2d2fba4a7e5f2d1fc4d684df7dd`; the generated `.sha256` sidecar matches this value.
- `scripts/Install-Memento.ps1` verified the sidecar, installed the app under `%LOCALAPPDATA%\MEMENTO\App`, and created the per-user Start Menu shortcut.
- `scripts/Start-Memento.ps1` launched the installed executable. A seven-second process smoke test observed `Memento.App`, `Responding=True`, and window title `MEMENTO`; the process then stopped cleanly.
- The installer copies `Uninstall-Memento.ps1` into the app tree and creates a current-user Windows Installed apps entry pointing at that stable path; no administrator access is required.
- The app-local uninstaller removes the shortcut and registry entry, then uses a temporary post-exit helper to delete its own app tree; archive data remains by default.
- Family Admin can rotate a known encrypted-backup password from the shell; the original `.memento` file is preserved and the re-encrypted copy is written only after source manifest verification.
- A real uninstall/reinstall exercise removed the app tree and registry entry, preserved the archive database byte-for-byte, and restored the install with the matching bundle sidecar.
- `scripts/Test-MementoPreflight.ps1` passed bundle/sidecar/checksum, installed executable, app-local uninstaller, app-lock recovery helper, shortcut, archive path, free-space, and stopped-process checks.
- Preflight also passed the Installed apps registration check, including its matching per-user `InstallLocation`.
- The preflight now checks the presence of the `MEMENTO/OpenAI` Credential Manager target without reading its value. This machine reports a warning because the target is absent; `-RequireCloudCredential` correctly makes that missing setup a blocking failure for a cloud-enabled pilot, while local-only capture remains usable.
- Preflight also checks the optional `MEMENTO/AppLock` target without reading its verifier; `-RequireApplicationLock` makes its absence blocking for deployments whose family policy requires the lock.
- `scripts/Setup-Memento.ps1` now composes publish, install, and preflight into one repeatable deployment command; `-SkipPublish` reuses an existing sidecar-verified bundle and `-Launch` starts the installed app only after checks pass. The script is parser-checked, and both the no-launch setup and `-Launch` process smoke path were exercised against the current verified bundle below.
- Installer rollback was exercised separately with an injected post-swap failure; the previous app tree was restored and archive data remained outside the app tree.
- Directory swaps retry short-lived Windows file locks before failing; this retry path was exercised with an isolated install root, then the default per-user installation was restored.
- The in-app `啟用本機錄音功能` setting persists the participant's recording enable/disable choice independently of per-session cloud consent.
- The app now exposes the latest transcript in a participant-facing clarification panel; it records speaker confirmation or uncertainty through the same provenance-preserving protocol used by the core tests.
- The participant shell now exposes `NORMAL`, `PRIVATE_CONVERSATION`, and `LOCAL_CAPTURE_ONLY` for the next recording. Private/local-only selections disable cloud consent and current-information queries, and service-boundary regressions prove that a private session cannot call cloud providers even if a cloud-consent row was mistakenly recorded.
- Capture start now records the cloud-consent event from the checkbox and privacy mode instead of assuming consent; a new core policy regression covers checked/unchecked and blocked-mode combinations.
- Processing now locks privacy, recording, and cloud-consent controls until the bounded cloud turn finishes, preventing a participant from changing policy while provider work is in flight.
- Active capture markers now update their WAV data length after each append, and recovery scanning can recover complete PCM frames left behind by a stale header after interruption.
- The optional application lock stores only a salted verifier in the current Windows user's Credential Manager. When configured it overlays the shell, disables archive controls, pauses the launch-time retry worker, and supports explicit unlock/disable flows; malformed existing credential blobs now fail closed and the pure/live Credential Manager regression tests pass, while target-machine recovery policy remains a supervised gate.
- A forgotten-passcode recovery helper (`scripts/Reset-MementoApplicationLock.ps1`) now refuses to run while MEMENTO is active, accepts only the fixed `MEMENTO/AppLock` target, supports PowerShell `-WhatIf`, and deletes only that credential while preserving archive data. The helper is parser-checked; a target-machine recovery drill is still pending.
- The installer also copies the recovery helper into `%LOCALAPPDATA%\MEMENTO\App` beside the app-local uninstaller, keeping the recovery path available after the repository is moved.

## Boundary and test verification

- Release tests pass **160/160**.
- The WinUI capture path now has an optional live Realtime device-chunk session. It requires a separate live-conversation consent, preserves local capture if the provider connection fails, and stores successful PCM output as a verified derived WAV; no live credential or physical device exchange was run.
- Realtime startup re-checks live consent and Source withdrawal after the transport handshake; if policy changes during connection setup, the session is disposed before capture can attach and a content-free failed provider interaction is recorded.
- Realtime completion has a two-minute default timeout; deterministic coverage verifies that a provider which never emits a terminal response fails with a content-free HTTP 504 instead of leaving the participant stop flow waiting indefinitely.
- `scripts/Build-MementoMsix.ps1` and `packaging/Package.appxmanifest.template.xml` now provide a parameterized MSIX staging/signing path. The staging script removes its intermediate publish directory after copying and fails if more than one root executable is staged, so the package contains one payload copy. Portable `Microsoft.Windows.SDK.BuildTools` `makeappx.exe` generated the latest unsigned package (**108,705,142 bytes**, SHA-256 `89C64A8D9385BAADC1E3EB0947790FE36E86C07DFA5DBBC809E833CE1F8AC679`), and `makeappx unpack` extracted it with one executable occurrence, a parseable manifest, no nested publish directory, and four package assets. A separate offline signing run (`-TimestampUrl ''`) produced a signed package (**108,727,137 bytes**, SHA-256 `7EA2BAEC80559E15D57E9A962D9B7867FDB5EBCA6A47C58CCEA36C01E47CAA82`); `signtool verify /pa` and signed-package unpack passed after temporary current-user trust. Current-user package deployment still returned `0x800B0109` because the Windows deployment service does not trust the non-admin development certificate; a publisher-owned certificate and target install/upgrade/uninstall verification remain pending.
- Extraction persistence now commits Evidence, candidate Claims, links, and search-index rows as one SQLite transaction; the rollback regression is included in the Release suite.
- Media-inclusive export now verifies snapshot-listed file presence and recorded length/SHA-256 metadata, with a regression that rejects tampered media and removes the partial bundle.
- `dotnet build Memento.slnx --configuration Release --no-restore` passes with **0 warnings and 0 errors**.
- The transcribed-text response and memory-extraction adapters neutralize marker-like strings inside participant text before adding the untrusted transcript wrapper; regression tests cover both adapters.
- The privacy boundary audit added four regression tests; the suite now verifies private-session blocking for bounded conversation, durable transcription, and durable extraction in addition to the existing local-only coverage.
- Automatic extraction queue creation also respects the persisted privacy mode, keeping private/local-only sessions free of cloud work items.
- Memory extraction rejects a supplied session whose privacy mode differs from the persisted session before provider execution; this closes the in-memory caller bypass and is covered by regression tests.
- Current-information queries now also re-check the selected next-recording privacy mode in the WinUI shell, so switching to a private or local-only mode cannot leave an earlier session's query control enabled.
- Selecting a cloud-blocked privacy mode also cancels any in-flight current-information request while revoking the visible consent control.
- `CurrentInformationService` now requires an explicit privacy mode and cloud-consent flag; blocked modes and missing consent fail before the search provider is called.
- The core clarification boundary rejects corrected wording on uncertainty, refusal, or “唔記得” outcomes; the regression confirms no corrected revision is written for that invalid input.
- WAV append recovery now derives the persisted data-byte count from the file length after each write, preserving bytes that reached a `.capture.tmp` marker before a storage failure.
- Restore extraction now uses a same-parent staging directory and leaves no partial target tree after unsafe or duplicate archive entries; failure-path regressions verify the target remains absent.
- The current bundle is portable and unpackaged. A signed MSIX still requires an owner-selected publisher identity, certificate, and package manifest.

## Gate decision

M14 remains **PARTIAL / AUTOMATED VERIFIED**. Native GUI interaction, physical microphone permission/disconnect behaviour, live provider credentials, Cantonese quality, authenticated Family Admin UX, target-machine encrypted restore, and a supervised participant pilot still require evidence on the target Windows machine. Computer Use tracking is intentionally not used as a blocker for this implementation pass.
