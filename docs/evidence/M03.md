# M03 evidence — provider boundary and bounded cloud path

**Date:** 2026-09-13  
**Status:** PARTIAL/BLOCKED pending credentialed live verification

## Scope

Keep local audio authoritative while allowing a future live voice provider to process only explicitly permitted content.

## Implementation

- `ConversationOrchestrator` accepts a finalized local audio path, privacy mode, and cloud consent. It rejects `LOCAL_CAPTURE_ONLY`, missing consent, or missing local evidence before provider transmission.
- `IConversationProvider`, `ITranscriptionProvider`, and `IReasoningProvider` keep vendor-specific transport and model objects outside the domain/archive.
- `provider_interactions` stores provider, capability, model, model snapshot, request ID, timestamps, coarse usage, success, and error metadata.
- `DeterministicConversationProvider` supports offline contract tests and is explicitly labelled as a test provider.
- `OpenAiTranscriptionProvider` provides an optional completed-audio HTTP adapter. It requires an injected credential, sends only a finalized local WAV, and does not persist credentials or raw error bodies. A realtime spoken-response adapter and secure client-token broker remain future work.
- `OpenAiResponsesProvider` and `BoundedVoiceConversationService` provide a turn-based fallback: finalized local audio is transcribed before a text response request, and `store=false` is sent for Responses calls. Generated response audio/TTS and Realtime transport remain future work.

## Official research re-check

On 2026-09-13, the [Realtime API reference](https://platform.openai.com/docs/api-reference/realtime?lang=javascript), [model catalogue](https://developers.openai.com/api/docs/models), and [GPT-Transcribe page](https://developers.openai.com/api/docs/models/gpt-transcribe) were re-read. The references describe current transport/model capabilities; they do not prove MEMENTO’s account configuration, retention settings, latency, or Cantonese accuracy.

## Automated verification

`dotnet test tests/Memento.Core.Tests/Memento.Core.Tests.csproj --configuration Release` passed **14/14** at the original M03 checkpoint; later adapter and worker tests bring the cumulative suite to **40/40**. Tests cover finalized-local-file ordering, consent and privacy gates, deterministic success metadata, provider failure metadata, HTTP request shape, and preservation of the local file.

## Gate decision

**PARTIAL/BLOCKED.** No live provider call was made because no credentialed integration was authorized or available. No successful cloud conversation is claimed.
