# M05 evidence — clarification and correction provenance

**Date:** 2026-09-13  
**Status:** PARTIAL/BLOCKED pending participant UX verification

## Scope

Make uncertainty visible, ask targeted questions for high-value ambiguity, and preserve every correction without rewriting original Source evidence.

## Implementation

- `ClarificationPolicy` weighs entity importance, ambiguity, confidence, and future-memory impact. It avoids asking about low-impact filler while escalating names, places, relationships, dates, identities, preferences, and major events.
- `ClarificationProtocol` stores an initial transcript revision, clarification question and participant response, an optional corrected revision linked to its parent, an explicit outcome, and a speaker-confirmed vocabulary entry when appropriate.
- Before writing a correction, the protocol verifies that the Source belongs to the supplied session and that the initial revision is persisted for that Source, preventing cross-session or orphaned provenance records.
- The protocol supports refusal, “唔記得”, two possibilities, and correction of a previous correction. A later vocabulary entry never changes the original revision or audio Source.

## Verification

`dotnet test tests/Memento.Core.Tests/Memento.Core.Tests.csproj --configuration Release` passed **23/23** at the original M05 checkpoint; the later ownership-hardening regression, post-revocation extraction, capture-shutdown recovery, and admin-review freshness tests bring the cumulative suite to **88/88**. Tests cover the `阿珍` → `阿貞` scenario, uncertain `小三定小四`, refusal, “唔記得”, repeated/corrected names, policy thresholds, rejection of cross-session or unpersisted initial revisions, and prevention of post-revocation candidate persistence.

## Gate decision

**PARTIAL/BLOCKED.** The audit chain is implemented and deterministic tests pass. Natural Cantonese wording, interruption handling, and participant acceptance still require a supervised UX session.
